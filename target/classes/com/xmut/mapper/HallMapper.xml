<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xmut.mapper.HallMapper">


    <!--==================================== 提供给其他Mapper ====================================-->
    <!-- Schedule反向关联-->
    <select id="findHallToSchedule" parameterType="Long" resultType="Hall">
        select * from t_hall where hall_id = #{hall_id}
    </select>
</mapper>